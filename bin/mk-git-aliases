#!/usr/bin/env bash
set -euxo pipefail

tgt=$HOME/dotfiles/zsh/git_aliases.zsh

echo "# Generated by script; do not edit" > $tgt
git la | grep -Ev '^g' | cut -d'=' -f1 | grep -v '-' | xargs -I{} echo 'alias g{}="git {}"' | tee -a $tgt

echo 'alias gg="git grep"' | tee -a $tgt

#!/usr/bin/env zsh
set -eo pipefail

# Simple script to check nix attrs for build status on Hydra
while [ -n "$1" ]; do
    chromium https://hydra.nixos.org/job/nixpkgs/trunk/$1.x86_64-linux
    shift
done
